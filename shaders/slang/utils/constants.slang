// It should match with the one defined in ConstantsAndStructs.hpp
static const uint MAX_OBJECTS = 1024;
static const uint MAX_TEXTURES = 16;

const static uint32_t MAX_RECURSION_DEPTH = 4;
const static half SHADOW_CONST = (half)0.3;